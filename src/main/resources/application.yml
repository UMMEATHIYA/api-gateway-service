eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://100.25.104.74:8761/eureka/
server:
  port: 9090
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: todo-service
          uri: http://100.25.104.74:8081/
          predicates:
            - Path=/todo-service/**
